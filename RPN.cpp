#include "RPN.h"

int ReversePolishNotation::evaluate () {
    int result;

    // The item on the top of the stack needs to be an operator.
    // We can do one of two things here:
    //      1.) Return an error code indicating that the data is bad
    //      2.) 

    return result;
} 
